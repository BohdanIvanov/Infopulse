function doSmth(){
	
	}

// обратного отсчёта
var timerId = window.setTimeout(doSmth, 1000)
window.clearTimeout(timerId);
	Результат №1:
	Начат обратный отсчёт
	Прошло N секунд
	Обратный отсчёт остановлен.

// интервальный
var timerId2 = window.setInterval(doSmth, 1000)
window.clearInterval(timerId2);
	Результат №2:
	Начат обратный отсчёт
	Прошло M секунд
	Прошло N секунд
	Прошло P секунд
	Обратный отсчёт остановлен.

	
var oWindow = window.open(sURL , sWindowName, sWindowParameters)
	1) sWindowName = "_blank" - много новых окон
	2) sWindowName = "blahblahblah" - много разных страниц в окне с именем blahblahblah
sWindowParameters = "a=1,b=2,..."			javascript window.open mdn
window.close();
oWindow.close();


класс divWindow
работает с HTML-блоком в котором есть:
- место для заголовка
- место для содержимого
- крестик для закрытия

методы класса:
- открыть окно
- закрыть окно

в класе mywindow будет свойство openWindowBtn
на него вешается событие click
которое выполняет метод mywindow.openWindow()
который вызывает метод divWindow.open()

объект класса divWindow нужно создать заранее и сохранить его в свойство
mywindow.customWindow = ...;